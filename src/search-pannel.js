import React from "react";

const SearchPannel = () => {
  const searchStyle = {
    fontSize: "25px",
  };
  return <input style={searchStyle} placeholder="search" />;
};

export default SearchPannel;
